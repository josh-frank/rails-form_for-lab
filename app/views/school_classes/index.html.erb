<h1>Show all school classes</h1>
<ul>
    <% @all_school_classes.each do | school_class | %>
        <li>
            <%= "#{ school_class.title } (Room #{ school_class.room_number })" %>
        </li>
    <% end %>
</ul>
